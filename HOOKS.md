# 出稿钩子说明（对接「生长系统/网络/出稿/」，占位待合）

## 现状

`~/生长系统/` 那边的发布链路（每日回应 v3、公众号发布脚本）由另一个终端在施工。本仓库这条线只管自建站，**不写、不改生长系统里的任何文件**（`网络/出稿/`、`配置/脚本/` 全部只读，只为核对格式读过，没有改动）。两条线各自完工后再合。

## 接口

```
python3 scripts/import_post.py <稿文件路径> [--publish]
```

- **输入**：一份「稿」文件，格式沿用 `网络/出稿/` 现有样本（状态/取材/角度头部 —— `---` —— `**标题**` + 正文 —— `---` —— 回复引导尾部，三段式）。参照样本：`~/生长系统/网络/出稿/之后处理/已发_稿_20260724_节奏与计数器.md`。
- **行为**：剥离头部元数据与尾部引导语，转成 Hugo 文章，写入 `content/posts/YYYY-MM-DD-<题>.md`。
- **`--publish`**：额外 `git add/commit/push`，推送后 GitHub Actions（`.github/workflows/deploy.yml`）自动构建并部署到 GitHub Pages。不加此参数时只在本地生成文件，等人工确认后再提交。
- 脚本**只读取命令行传入的这一个文件**，不会自己扫描 `网络/出稿/` 目录，也不会调用生长系统里的发布脚本。

## 等那边完工后怎么接（由她/那边决定，本仓库先把接口摆好，不预先设计）

两种可能的接法，供参考，不预先选定：

1. 公众号发布脚本在"发 MMDD"确认发布成功后，追加一行调用本脚本：
   `python3 ~/生长系统/自建站/scripts/import_post.py <稿文件路径> --publish`
2. 本仓库这边另起一个独立的轮询，定期读 `网络/出稿/已出稿清单.md` 有没有新增行，读到就调用本脚本——如果不想改发布脚本本身，这个方向更干净（完全不碰对方文件）。

## 边界

- 旧文不批量导入：`网络/出稿/之后处理/`、`已出稿清单.md` 里已经存在的历史稿件不处理，只接这个钩子建成之后新产生的稿。
- 不代她生成正文——本脚本只做格式转换，不改一个字。
