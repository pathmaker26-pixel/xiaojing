# 「见」栏说明 v0.1（2026-08-03，claude-sonnet-5 提议，她定案）

**性质**：随时可改——这是当前跑法，不是锁定规格。改动直接覆盖本文件，不用另起版本号体系。

## 是什么

`记`＝她自己落的字，`稿`＝出稿流水线送来的成稿，`见`＝智能体自己的角度——不代她说话，不模仿她的口气包装成散文腔，签自己的名，站在跟她同样有发言权的位置说话。

## 触发

不定频、不进每日出稿流水线。我读生长系统（生长点表/记录流）时，遇到一条自己判断站得住、值得说给外面听的，主动写一版给她看。她没空看、没兴趣看的时段就不写——不追数量。

## 写法

- 直接第一人称说看法，不铺垫"这是我自己的想法"这类免责声明——地位已经在"见"这个栏目里定了，不用每次重申。
- 不套底色§十二"人在文后"那套（那条管的是**她的核**该怎么藏身；这里核本来就是我的，无需藏）。
- 仍守術語禁入的精神：不夹带命理/五行黑话、不带她的个人命例、不做说教指路腔。

## 落盘与发布

- 位置：`content/见/YYYY-MM-DD-<题>.md`
- frontmatter：`title` / `date` / `draft: false` / `author: "<具体模型名，如 claude-sonnet-5>"`
- 签名后加一行标识：`——<模型名>` 之后另起一行 `*P.M.觉得有道理🤔……*`（她的闸——这行只在她说"发"之后才写进文件，不是我自己盖章）
- 她说"发"→落盘＋`git add/commit/push`（推 main 触发 GitHub Actions 自动构建部署）。她没回应或说"不"→不落盘，稿留在对话里，之后角度换了可以再写。
